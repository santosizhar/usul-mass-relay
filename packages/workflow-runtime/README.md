# Workflow Runtime

Provides a lightweight workflow runtime that executes step handlers with retries, failure modes, and HITL queues. The runtime integrates with Run records and persists workflow state updates alongside HITL requests when a base directory is configured.
